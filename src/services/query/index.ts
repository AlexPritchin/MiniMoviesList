import { logInUser, registerUser, addMovie, deleteMovie } from './mutations';
import { getMoviesList } from './queries';

export {
  logInUser,
  registerUser,
  addMovie,
  deleteMovie,

  getMoviesList,
};
